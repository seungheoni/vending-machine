

jar {
    enabled = true
    archiveName 'DRINK.API.jar'
}

version = 'v01'

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'

    testImplementation 'org.testcontainers:mongodb:1.17.2'
}
