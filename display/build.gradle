bootJar {
    enabled = true
    archiveName 'DRINK.DISPLAY.jar'
}

version = 'v01'

dependencies {

    implementation project(':core')

    implementation 'org.springframework.boot:spring-boot-starter-web'
}
