drink:
  product:
      list: 콜라,사이다,식혜
spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017


logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG